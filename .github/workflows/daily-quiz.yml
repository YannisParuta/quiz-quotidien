name: Generate Daily Quiz Questions

on:
  schedule:
    # S'exécute tous les jours à minuit (UTC)
    - cron: '0 0 * * *'
  # Permet aussi de lancer manuellement
  workflow_dispatch:

jobs:
  generate-questions:
    runs-on: ubuntu-latest
    
    steps:
      - name: Call Vercel API to generate questions
        run: |
          echo "🤖 Génération de 10 nouvelles questions..."
          response=$(curl -s -w "\n%{http_code}" https://quiz-quotidien.vercel.app/api/generate-questions)
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "📊 Réponse API: $body"
          
          if [ "$http_code" -ne 200 ]; then
            echo "❌ Erreur: HTTP $http_code"
            exit 1
          fi
          
          echo "✅ Questions générées et version incrémentée automatiquement"
      
      - name: Summary
        run: |
          echo "### ✅ Génération quotidienne terminée" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 10 nouvelles questions ajoutées" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Version incrémentée dans questions.json" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Cache utilisateurs sera vidé automatiquement" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📅 Date: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
